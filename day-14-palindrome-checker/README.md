
# Day 14 - Pallindrome checker

A simple terminal palindrome checker for strings

## What it does
- User inputs a string
- computer checks if its the same string if reversed
- if not its not a pallindrome

## What I learned
- user input
- if /else condition
- [::-1] string slicing for reversing

## Run it
python.py
