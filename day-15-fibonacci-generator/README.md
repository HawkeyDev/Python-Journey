
# Day 15 - Fibonacci Generator

A simple terminal genarator used to get fibonacci numbers in series as per user input.

## What it does
- user gives the input for numbers to be generated
- a, b = b, a + b — the swap pattern for fibonacci series.
- fibonacci numbers is the sum of previous number and the next number
- The output of fibonacci series is printed

## What I learned
- for loop to generate a value in series 
- taking user input
  
## Run it
python.py
